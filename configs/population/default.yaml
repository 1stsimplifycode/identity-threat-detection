# Full-scale population profile (~thousands of users). Role/department
# vocabulary itself (job titles, geo city pools) lives in code
# (generator/constants.py, preprocessing/geo_utils.py) as reference
# vocabulary, analogous to the MITRE technique-name table -- everything here
# is a tunable ratio or count, per the "config-driven" design constraint.

num_users: 8000

department_weights:
  Engineering: 0.20
  Sales: 0.15
  Operations: 0.12
  IT: 0.10
  Customer Support: 0.12
  Finance: 0.08
  Marketing: 0.08
  HR: 0.06
  Legal: 0.04
  Executive: 0.05

privilege_distribution:
  standard: 0.82
  elevated: 0.12
  admin: 0.055
  domain_admin: 0.005

work_mode_distribution:
  onsite: 0.45
  hybrid: 0.40
  remote: 0.15

termination_rate: 0.03

home_countries: ["US", "GB", "DE", "IN", "CA", "AU", "BR", "JP", "SG", "FR"]
