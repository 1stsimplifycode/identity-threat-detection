"""Shared canonical vocabularies, geo utilities, and schema validation.

This package holds reference data and validation logic used by both
`generator/` (to build data) and `evaluation/`/`tests/` (to check it) -- kept
here rather than duplicated so generation and validation can never drift
apart silently.
"""
